"
Subclasses are obsolete classes that must be converted when read from BOSS.

Instance Variables:

"
Class {
	#name : #Obsolete,
	#superclass : #Object,
	#category : #'StCAD-Geo-Obsolete classes'
}

{ #category : #'VisualWorks metadata' }
Obsolete class >> visualWorksMetadata [

	^ #(
		'namespace' 'StCAD.Geo'
	)

]
